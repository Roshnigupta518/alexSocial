<resources>

    <!-- Base application theme. -->
    <!-- <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar"> -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
           <!-- Apply custom date picker style -->
        <item name="android:datePickerDialogTheme">@style/CustomDatePicker</item>
         <!-- Customize your theme here. -->
    <item name="android:textColor">#000000</item>
    </style>

      <style name="CustomDatePicker" parent="android:Theme.Material.Light.Dialog">
     <item name="colorAccent">#FCE058</item>          <!-- Works on Android <29 -->
        <item name="android:colorAccent">#FCE058</item>  <!-- Explicit for newer versions -->
   </style>

</resources>
